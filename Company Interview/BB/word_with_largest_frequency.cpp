#include <string>
#include <vector>
#include <algorithm>


class Solution {
public:
    string highestFrequencyWord(vector<string>& words) {
        sort(words.begin(), words.end());
        int start = 0; end = 1
        for (int i = 0; i < words.size(); i++) {

        }
    }
}